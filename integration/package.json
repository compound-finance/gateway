{
  "name": "gateway-integration-tests",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "@polkadot/api": "^4.11.2",
    "chalk": "^4.1.0",
    "ganache-core": "^2.13.2",
    "getopts": "^2.3.0",
    "solidity-parser-antlr": "https://github.com/solidity-parser/parser.git",
    "web3": "^1.3.1"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.14.2",
    "babel-jest": "^27.0.1",
    "jest": "^26.6.3",
    "jest-junit": "^12.0.0"
  },
  "scripts": {
    "postinstall": "cat /dev/null > node_modules/source-map-support/register.js",
    "test": "PROFILE=release QUIET_SCENARIOS=true node --experimental-vm-modules node_modules/.bin/jest",
    "build": "yarn build:ethereum && yarn build:cargo",
    "build:ethereum": "(cd ../ethereum && yarn compile)",
    "build:cargo": "WASM_BUILD_RUSTFLAGS='--cfg feature=\"integration\"' cargo +nightly build --release --features integration && echo \"removing compact for now, todo: fix this\" && rm ../target/release/wbuild/gateway-runtime/*compact*",
    "console": "NODE_OPTIONS='--experimental-repl-await' npx saddle console"
  },
  "resolutions": {
    "solidity-parser-antlr": "https://github.com/solidity-parser/parser.git",
    "**/source-map-support": "=0.5.19"
  }
}
